{
  "name": "EduBot-1",
  "version": "1.0.0",
  "description": "First of all, rename `hidden.env` to `.env`. Then get your discord bot token and replace `my-token-here` with it. Optionally, move to `index.js` and edit lines 6-12 (client configuration) as it suits you. Your bot is ready to launch!",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@replit/database": "^2.0.1",
    "discord.js": "^12.5.3",
    "express": "^4.17.1"
  }
}
